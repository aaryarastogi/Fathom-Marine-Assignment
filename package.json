{
  "name": "fathom-marine-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "NPM_CONFIG_PRODUCTION=false npm install --prefix server && NPM_CONFIG_PRODUCTION=false npm install --prefix client",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm start --prefix server"
  }
}
